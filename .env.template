# Gemini API Key
GEMINI_API_KEY=your_api_key_here

# Model Routing
# Lightweight operations (classification, intent, quick synthesis)
QUERY_MODEL=gemini-2.5-flash

# Deep reasoning (complex analysis, evidence extraction, reports)
REASONING_MODEL=gemini-2.5-pro

# Primary model (fallback for older configs)
PRIMARY_LLM_MODEL=gemini-2.5-pro

# LLM Configuration
LLM_TEMPERATURE=0.1
LLM_PROVIDER=gemini
